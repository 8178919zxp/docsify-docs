# 数据集处理方法

## 图片数据集增加标签

~~~py
import os

roor_dir = "hymenoptera_data/train"
target_dir = "ants_image"
img_path = os.listdir(os.path.join(roor_dir,target_dir))
"""
获取ants_image目录中所有文件的列表：os.path.join(roor_dir,target_dir) 拼接路径："hymenoptera_data/train/ants_image"
                               os.listdir() 列出该目录中的所有文件名
"""
label = target_dir.split('_')[0]
"""
从"ants_image"中提取标签：
split('_') 将字符串按"_"分割成["ants", "image"]
[0] 取第一个元素"ants"
"""
out_dir = "ants_label"
for i in img_path:
    file_name = i.split('.jpg')[0]
    """
    从"ants_image"中提取标签：
    split('_')将字符串按"_"分割成["ants", "image"]
    [0]取第一个元素"ants"
    """
    with open(os.path.join(roor_dir,out_dir,"{}.txt".format(file_name)),'w') as f:
        f.write(label)
    """
    创建并打开标签文件：
          构建路径："hymenoptera_data/train/ants_label/ant001.txt"
          'w' 模式表示写入模式（如果文件存在则覆盖）
          with open() 确保文件正确关闭
    """

~~~

## Dataset图片数据集的读取

这里的数据集结构为：

```
hymenoptera_data/train/
    ├── ants/
    │   ├── ant001.jpg
    │   ├── ant002.jpg
    │   └── ...
    └── bees/ 
    │   ├── bee001.jpg
    │   ├── bee002.jpg
    │   └── ...
hymenoptera_data/val/
    ├── ants/
    │   ├── ant001.jpg
    │   ├── ant002.jpg
    │   └── ...
    └── bees/ 
    │   ├── bee001.jpg
    │   ├── bee002.jpg
    │   └── ...
```

~~~python
from torch.utils.data import Dataset
from PIL import Image
import os
class MyData(Dataset):

    def __init__(self,root_dir,label_dir): # self的作用是类里的全局变量
        self.root_dir =root_dir
        self.label_dir = label_dir
        self.path = os.path.join(self.root_dir,self.label_dir)
        self.img_path = os.listdir(self.path)
    def __getitem__(self, idx):
        img_name = self.img_path[idx]
        img_item_path = os.path.join(self.root_dir,self.label_dir,img_name)
        img = Image.open(img_item_path)
        label = self.label_dir
        return img,label
    def __len__(self):
        return len(self.img_path)

root_dir="hymenoptera_data/train"
ants_label_dir = "ants"
bees_label_dir = 'bees'
bees_dataset = MyData(root_dir,bees_label_dir)
ants_dataset = MyData(root_dir,ants_label_dir)
train_dataset = ants_dataset +bees_dataset
~~~

